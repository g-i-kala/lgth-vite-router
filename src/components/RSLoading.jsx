import styles from "../styles";

const RSLoading = () => {
  return (
    <div className={`flex items-center justify-center h-screen`}>
        <div className={`text-center ${styles.heading1}`}>
            The wind is about to pick up..
        </div>
    </div>
  )
}

export default RSLoading